import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  default: {
    width: 56,
    height: 56,
    borderRadius: 5,
    backgroundColor: '#C71861',
    alignItems: 'center',
    justifyContent: 'center',
  },

});

export default styles;
